# Copy to .env and fill in your TDX API credentials.
TDX_CLIENT_ID=your_client_id_here
TDX_CLIENT_SECRET=your_client_secret_here

# Optional: Open Data sources may require API keys or auth headers.
# You can reference this from config via open_data.sources[].headers_env
# Example: headers_env: { authorization: "OPENDATA_AUTHORIZATION" }
OPENDATA_AUTHORIZATION=
